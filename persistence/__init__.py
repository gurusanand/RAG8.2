"""
Persistence Layer — MongoDB document store and FAISS vector store.
Provides persistent storage for uploaded documents, chunks, and embeddings
so they survive server restarts without re-uploading or re-embedding.
"""
